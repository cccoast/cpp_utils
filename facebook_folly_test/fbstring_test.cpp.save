//##################################################################################
//###@author xudi                                                                  #
//###@Mail   xudi1989@ruc.edu.cn                                                   #
//###@Lisence GPL                                                                  #
//##################################################################################

#include <iostream>
#include <memory>
#include <functional>
#include <thread>
#include <chrono>

#include <unistd.h>

#include <folly/FBString.h>
#include <folly/FBVector.h>>
#include <folly/Foreach.h>

using namespace std;

int fbstring_test(){

    folly::fbstring s("xudi");
    s.append(",hello");
    s = "abcabcdabcdefg";

    folly::fbstring toberep("a");
    folly::fbstring rep("x");
    auto iter = 0;
    while( ( iter = s.find(toberep,iter) ) >= 0){
        cout << iter << " -> " << s[iter] << endl;
        s.replace(iter,toberep.length(),rep);
        cout << s << endl;
    }
    cout << s << endl;
    return 0;
}

int main(){

    folly::fbvector<int> v({1,2,3,4,5,6});
    v.insert(v.end(),10);
    folly::FOR_EACH(i,v){
        cout << i << endl;
    }
    return 0;
}
